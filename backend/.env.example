# Copy this to .env and fill in your values.
# Get both from: Supabase dashboard → Project Settings → API

# Your project's public URL
SUPABASE_URL=https://rpmgcurhxrgtzbdixtay.supabase.co

# Service role key — server-side only, never expose to the frontend.
# This key bypasses Row Level Security, which is what the import script needs.
SUPABASE_SERVICE_ROLE_KEY=sb_publishable_uZimzo_tWiqShsDX0_ROOw_HURMMQs1
